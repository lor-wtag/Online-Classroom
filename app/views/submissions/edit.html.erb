<h3>Edit Submission for <%= @assignment.title %></h3>

<%= form_with(model: @submission, url: classroom_assignment_submission_path(@classroom, @assignment, @submission), local: true) do |form| %>
  <% if @submission.errors.any? %>
    <div class="alert alert-danger">
      <h4>There were some errors:</h4>
      <ul>
        <% @submission.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% if can? :update, @submission %>
    <div class="form-group mb-3">
      <%= form.label :files, class: 'form-label' %>
      <%= form.file_field :files, multiple: true, class: 'form-control' %>
    </div>

    <% if @submission.files.attached? %>
      <h4 class="mt-4">Current Files:</h4>
      <% @submission.files.each_with_index do |file, index| %>
        <div class="file mb-2">
          <%= link_to file.filename.to_s, rails_blob_path(file, disposition: 'inline'), target: '_blank', class: 'btn btn-link' %>
          <%= check_box_tag "submission[remove_files][]", file.id, false, class: 'form-check-input' %> 
          <label class="form-check-label" for="submission_remove_files_<%= file.id %>">Remove</label>
        </div>
      <% end %>
    <% end %>
  <% end %>

  <div class="form-group mb-3">
    <%= form.submit 'Update Submission', class: 'btn btn-success' %>
  </div>
<% end %>

<%= link_to 'Cancel', classroom_assignment_submission_path(@classroom, @assignment, @submission), class: 'btn btn-secondary' %>
